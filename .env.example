# 複製為 .env 後填入實際值，.env 本身不進版控。

# 審核後台（/admin/review）與相關 API 的 HTTP Basic Auth，見 proxy.ts。
# 未設定時 proxy.ts 會直接回絕（503），不會靜默放行。
ADMIN_USER=
ADMIN_PASSWORD=

# Phase 5 起，UGC 投稿資料改存 PostgreSQL（取代 data/contributions.json）。
# 格式：postgresql://<user>:<password>@<host>:<port>/<database>
# 首次部署需先執行 `pnpm run db:migrate` 建立資料表，見 配音擂台-infra.md。
DATABASE_URL=

# Phase 7 起，具名審核者帳號（取代單一共用密碼的 Basic Auth，見上方 ADMIN_USER）。
# 用來簽章 session cookie 的隨機字串，建議用 `openssl rand -hex 32` 產生。
# 未設定時 /admin/login 會回絕（503），仍可用上方 Basic Auth 登入，不影響既有流程。
# 設定後用 `pnpm run reviewer:create` 建立第一個審核者帳號，見 配音擂台-infra.md。
SESSION_SECRET=
